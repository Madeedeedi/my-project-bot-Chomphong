module.exports = {
  // ============ กันแสปม ============
  // จำนวนข้อความสูงสุดที่ส่งได้ในหน้าต่างเวลา rateWindow (ms)
  maxMessages: 5,
  // ระยะเวลาหน้าต่างเวลา (ms) ที่นับจำนวนข้อความ
  rateWindow: 5000,
  // ระยะเวลา mute (ms) เมื่อถูกเจอว่าแสปม
  muteDuration: 60000,
  // จำนวนตัวอักษรสูงสุดต่อข้อความ
  maxMessageLength: 2000,
  // ระยะเวลาตรวจข้อความซ้ำ (ms)
  duplicateWindow: 10000,

  // ============ ลบข้อความแสปม ============
  // จำนวนข้อความย้อนหลังสูงสุดต่อช่องที่ตรวจ/ลบ
  purgePerChannel: 100,
  // ลบเฉพาะข้อความแสปมในระยะเวลา (ชั่วโมง) นี้เท่านั้น
  purgeWindowHours: 1,

  // ============ แบนช่องต้องห้าม ============
  // 🔥 Channel ID ที่ห้ามพิมพ์ข้อความ (ลบ + แบนถาวร)
  bannedChannels: ["1545773474184822856"],

  // ============ สิทธิ์ทีมงาน ============
  // Role ID ของทีมงาน/Admin (ถ้าว่าง → ใช้ Admin/ManageGuild)
  staffRoleIds: [],
};